const puppeteer = require("puppeteer"); (async () => { const browser = await puppeteer.launch({ headless: "new" }); const page = await browser.newPage(); await page.goto("https://dsvn.vn/#/timkiem/ketqua", { waitUntil: "networkidle2" }); page.on("request", (req) => { if (req.url().includes("GetChosByToaXeId")) { console.log("Headers:", req.headers()); } }); await page.evaluate(() => { fetch("https://dsvn.vn/api/BanVeWeb/GetChosByToaXeId", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ DMTauId: 57275, ToaXeId: "An20", BookingCode: "" }) }).then(res => console.log(res.status)).catch(e => console.error(e)); }); await new Promise(r => setTimeout(r, 5000)); await browser.close(); })();
